# Antigravity Assistant Rules

## Browser & Server Constraints
- **Never open or control the browser**: Do NOT launch browser subagents, open URLs, or capture screenshots unless explicitly requested by the user.
- **Never start background dev servers**: Do NOT run `npm run dev` or launch background server processes automatically unless explicitly requested by the user.
